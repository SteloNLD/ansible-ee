version: 3

images:
  base_image:
    name: quay.io/ansible/community-ee-base:latest

dependencies:
  python:
    - ansible-lint
  galaxy:
    collections:
      - name: community.general
      - name: community.crypto
      - name: ansible.posix
