version: 3

images:
  base_image:
    name: ubuntu:24.04

dependencies:
  python:
    - ansible-core
    - ansible-lint
  galaxy:
    collections:
      - name: community.general
      - name: community.crypto
      - name: ansible.posix
